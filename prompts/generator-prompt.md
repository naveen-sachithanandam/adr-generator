## Task

Generate a complete **Architecture Decision Record** from the inputs below. The output must follow the **MADR-inspired** structure and rules from your system instructions (exact `##` headings, `#` title line, grounding rules).

### Rough notes
{notes}

### Additional context
{context}

### Optional metadata
Use only facts the user supplied. If a field is “Not provided”, do not invent values for that dimension.

| Field | Value |
|-------|--------|
| Domain | {domain} |
| Scale | {scale} |
| Constraints | {constraints} |
| Tradeoffs | {tradeoffs} |

Produce the full ADR Markdown now.
